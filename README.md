# stapp-website

Simple static website for Azure Static Web Apps.

## Local preview

Open `index.html` directly in a browser, or run a local static server:

```powershell
python -m http.server 8080
```

Then open `http://localhost:8080`.

## Deploy target

This repository is structured as a root static site. The main entry point is `index.html`.
